function sum(a, b) {
    // do things with the input and return n output 
    const sumValue = a + b;
    return sumValue;
}

const value = sum(1, 2)
const value2 = sum(1, 10)
console.log(value);